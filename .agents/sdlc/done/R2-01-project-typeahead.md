# Fix Project Selector - Convert to Typeahead

## Problem
The project search is split into a separate input above the dropdown. Should be a single typeahead/combobox.

## Solution
Replace the separate Input + Select with a single ComboBox component that filters as you type.
